<h1>Reset Password</h1>

<%= form_with @user, :url => password_reset_path(params[:id]), class: 'e-form e-form-insert' do |f| %>
  <%= f.password_field :password, placeholder: "new password", class: 'e-input' %> 
  <%= f.submit 'Update password', class: 'e-button e-button-submit' %>
<% end %>