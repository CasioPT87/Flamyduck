<!DOCTYPE html>
<html>
  <head>
    <title>Flamy Duck</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <link rel="shortcut icon" href=<%= image_url('favicon.ico') %> type="image/x-icon" />
    <link rel="preconnect" href="https://fonts.gstatic.com">
    <link href="https://fonts.googleapis.com/css2?family=Rubik&display=swap" rel="stylesheet">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <%= render "/layouts/top_bar" %>
    <div class="<%= ['o-container', *yield(:container_classes)].join(' ') %>">
      <% if logged_in? %>
        <p class="c-text c-text__user_name u-padding--6">Hi, <%= current_user.name %></p>
      <% end %>
      <%= yield %>
    </div>
    <%= render "/notifications/cookie" %>
  </body>
</html>
